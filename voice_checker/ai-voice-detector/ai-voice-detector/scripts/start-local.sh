#!/bin/bash

# Start the AI Voice Detector module in a local development environment

# Navigate to the project directory
cd "$(dirname "$0")/.."

# Install dependencies
npm install

# Start the application
npm run start